# Coupon / media fixtures
#
# Live demos prefer a public HTTPS URL via COUPON_MEDIA_URL (.env).
# This folder documents the expected media use-case (PDF coupon / image).
# Unit tests mock the Graph API and do not upload this file.

Place a sample PDF or image here for local reference, or keep using the
W3C dummy PDF URL from `.env.example`.
